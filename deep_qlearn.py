#Combination of q learing with neural networks(deep learning )
# Here the inputs to the neural networks are that states of our environment and outputs are the q values associated to each action of that input state
# Predict q value with NN rather than with Bellman equation! :) 